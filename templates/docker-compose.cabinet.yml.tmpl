services:
  cabinet-frontend:
    image: ghcr.io/bedolaga-dev/bedolaga-cabinet:latest
    container_name: cabinet_frontend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${CABINET_LOCAL_PORT}:80"
